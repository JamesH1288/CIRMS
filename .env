# Frontend Configuration for API Calls
REACT_APP_API_URL=http://localhost:3000  # This should match the backend's port

# Backend Port (if needed for local references, optional for frontend)
PORT=3000
